diff --git a/client/adb_wifi_termux_stub.cpp b/client/adb_wifi_termux_stub.cpp
new file mode 100644
index 0000000..9859307
--- /dev/null
+++ b/client/adb_wifi_termux_stub.cpp
@@ -0,0 +1,13 @@
+/*
+ * Pairing is not available in the Android-target TCP-only adb build.
+ */
+
+#include "adb_wifi.h"
+
+void adb_wifi_pair_device(const std::string&, const std::string&, std::string& response) {
+    response = "adb pair is unavailable in this Android build";
+}
+
+bool adb_wifi_is_known_host(const std::string&) {
+    return false;
+}
diff --git a/client/commandline.cpp b/client/commandline.cpp
index 5cd3ab6..2b42645 100644
--- a/client/commandline.cpp
+++ b/client/commandline.cpp
@@ -65,7 +65,6 @@
 #include "bugreport.h"
 #include "client/file_sync_client.h"
 #include "commandline.h"
-#include "fastdeploy.h"
 #include "incremental_server.h"
 #include "services.h"
 #include "shell_protocol.h"
diff --git a/client/fastdeploy.h b/client/fastdeploy.h
index 830aeb2..79a90fb 100644
--- a/client/fastdeploy.h
+++ b/client/fastdeploy.h
@@ -18,7 +18,13 @@
 
 #include "adb_unique_fd.h"
 
+#if defined(ANDROID_TOOLS_TERMUX_ADB)
+namespace com::android::fastdeploy {
+struct APKMetaData {};
+}  // namespace com::android::fastdeploy
+#else
 #include "fastdeploy/proto/ApkEntry.pb.h"
+#endif
 
 #include <optional>
 #include <string>
diff --git a/client/fastdeploy_termux_stub.cpp b/client/fastdeploy_termux_stub.cpp
new file mode 100644
index 0000000..57a249e
--- /dev/null
+++ b/client/fastdeploy_termux_stub.cpp
@@ -0,0 +1,27 @@
+/*
+ * Minimal fastdeploy stubs for Android-target TCP-only adb builds.
+ */
+
+#include "fastdeploy.h"
+
+void fastdeploy_set_agent_update_strategy(FastDeploy_AgentUpdateStrategy) {}
+
+int get_device_api_level() {
+    return 0;
+}
+
+std::optional<com::android::fastdeploy::APKMetaData> extract_metadata(const char*) {
+    return std::nullopt;
+}
+
+unique_fd install_patch(int, const char**) {
+    return unique_fd();
+}
+
+unique_fd apply_patch_on_device(const char*) {
+    return unique_fd();
+}
+
+int stream_patch(const char*, com::android::fastdeploy::APKMetaData, unique_fd) {
+    return -1;
+}
diff --git a/client/main.cpp b/client/main.cpp
index cdecc01b..b7bf4b2c 100644
--- a/client/main.cpp
+++ b/client/main.cpp
@@ -40,7 +40,9 @@
 #include "client/mdns_utils.h"
 #include "client/transport_client.h"
 #include "client/usb.h"
+#if !defined(__ANDROID__) || __ANDROID_API__ >= 30
 #include "client/usb_libusb_hotplug.h"
+#endif
 #include "commandline.h"
 #include "sysdeps/chrono.h"
 #include "transport.h"
@@ -140,11 +142,15 @@ int adb_server_main(int is_daemon, const std::string& socket_spec, const char* o
     }
 
     if (!getenv("ADB_USB") || strcmp(getenv("ADB_USB"), "0") != 0) {
+#if defined(__ANDROID__) && __ANDROID_API__ < 30
+        usb_init();
+#else
         if (is_libusb_enabled()) {
             libusb::usb_init();
         } else {
             usb_init();
         }
+#endif
     } else {
         adb_notify_device_scan_complete();
     }
diff --git a/client/usb_termux_stub.cpp b/client/usb_termux_stub.cpp
new file mode 100644
index 0000000..5588c04
--- /dev/null
+++ b/client/usb_termux_stub.cpp
@@ -0,0 +1,16 @@
+/*
+ * Minimal USB stubs for Android-target adb builds that only use TCP/IP transport.
+ */
+
+#include "client/usb.h"
+#include "transport.h"
+
+void usb_init() {}
+
+void usb_cleanup() {}
+
+bool is_libusb_enabled() {
+    return false;
+}
+
+void init_usb_transport(atransport*, usb_handle*) {}
