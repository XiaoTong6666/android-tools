diff --git a/CMakeLists.txt b/CMakeLists.txt
index 8a550ae..faea449 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -12,9 +12,16 @@ option(ANDROID_TOOLS_PATCH_VENDOR "Patch vendor projects using patches directory
 option(ANDROID_TOOLS_USE_BUNDLED_FMT "Use bundled fmt library instead of system provided one" OFF)
 option(ANDROID_TOOLS_USE_BUNDLED_LIBUSB "Use bundled libusb library instead of system provided one" OFF)
 option(ANDROID_TOOLS_LIBUSB_ENABLE_UDEV "Enable udev for device enumeration and hotplug support" OFF)
+option(ANDROID_TOOLS_TERMUX_ADB "Apply the Termux adb/fastboot hooks and link libtermuxadb" OFF)
+option(ANDROID_TOOLS_BUILD_ONLY_ADB "Only configure the adb target and its direct dependencies" OFF)
+set(ANDROID_TOOLS_TERMUX_LINK_DIR "" CACHE PATH "Additional library search directory for Termux adb builds")
 
 # Install bash/zsh completion files.
-set(COMPLETION_COMMON_DIR "${CMAKE_INSTALL_FULL_DATADIR}/android-tools/completions")
+if(ANDROID_TOOLS_TERMUX_ADB)
+	set(COMPLETION_COMMON_DIR "${CMAKE_INSTALL_FULL_DATADIR}/termux-adb/completions")
+else()
+	set(COMPLETION_COMMON_DIR "${CMAKE_INSTALL_FULL_DATADIR}/android-tools/completions")
+endif()
 add_subdirectory(completions)
 
 # Android provides it's own version of mke2fs which is incompatible with
diff --git a/vendor/CMakeLists.adb.txt b/vendor/CMakeLists.adb.txt
index 8a5bbf3..24cb93e 100644
--- a/vendor/CMakeLists.adb.txt
+++ b/vendor/CMakeLists.adb.txt
@@ -6,29 +6,30 @@ protobuf_generate_cpp(ADB_KEY_TYPE_PROTO_SRCS ADB_KEY_TYPE_PROTO_HDRS
 	adb/proto/key_type.proto)
 protobuf_generate_cpp(ADB_KNOWN_HOSTS_PROTO_SRCS ADB_KNOWN_HOSTS_PROTO_HDRS
 	adb/proto/adb_known_hosts.proto)
-protobuf_generate_cpp(ADB_PAIRING_PROTO_SRCS ADB_PAIRING_PROTO_HDRS
-	adb/proto/pairing.proto)
+
+if(NOT ANDROID_TOOLS_TERMUX_ADB)
+	protobuf_generate_cpp(ADB_PAIRING_PROTO_SRCS ADB_PAIRING_PROTO_HDRS
+		adb/proto/pairing.proto)
+endif()
 
 # No fastdeploy because it requires deployagent.inc
 set(libadb_SOURCES
 	adb/client/adb_client.cpp
 	adb/client/adb_install.cpp
-	adb/client/adb_wifi.cpp
 	adb/client/auth.cpp
 	adb/client/bugreport.cpp
 	adb/client/commandline.cpp
 	adb/client/console.cpp
+	adb/client/detach.cpp
 	adb/client/file_sync_client.cpp
 	adb/client/incremental.cpp
 	adb/client/incremental_server.cpp
 	adb/client/incremental_utils.cpp
 	adb/client/line_printer.cpp
 	adb/client/main.cpp
-	adb/client/pairing/pairing_client.cpp
-	adb/client/usb_libusb.cpp
-	adb/pairing_auth/aes_128_gcm.cpp
-	adb/pairing_auth/pairing_auth.cpp
-	adb/pairing_connection/pairing_connection.cpp
+	adb/client/mdns_utils.cpp
+	adb/client/mdns_disabled.cpp
+	adb/client/transport_emulator.cpp
 	adb/services.cpp
 	adb/socket_spec.cpp
 	adb/sysdeps_unix.cpp
@@ -37,15 +38,33 @@ set(libadb_SOURCES
 	${ADB_APP_PROCESSES_PROTO_SRCS} ${ADB_APP_PROCESSES_PROTO_HDRS}
 	${ADB_HOST_PROTO_SRCS} ${ADB_HOST_PROTO_HDRS}
 	${ADB_KNOWN_HOSTS_PROTO_SRCS} ${ADB_KNOWN_HOSTS_PROTO_HDRS}
-	${ADB_KEY_TYPE_PROTO_SRCS} ${ADB_KEY_TYPE_PROTO_HDRS}
-	${ADB_PAIRING_PROTO_SRCS} ${ADB_PAIRING_PROTO_HDRS})
+	${ADB_KEY_TYPE_PROTO_SRCS} ${ADB_KEY_TYPE_PROTO_HDRS})
 
-if(APPLE)
+if(NOT ANDROID_TOOLS_TERMUX_ADB)
 	list(APPEND libadb_SOURCES
-		adb/client/usb_osx.cpp)
+		adb/client/adb_wifi.cpp
+		adb/client/pairing/pairing_client.cpp
+		adb/client/usb_libusb.cpp
+		adb/client/usb_libusb_device.cpp
+		adb/client/usb_libusb_hotplug.cpp
+		adb/client/usb_libusb_inhouse_hotplug.cpp
+		adb/pairing_auth/aes_128_gcm.cpp
+		adb/pairing_auth/pairing_auth.cpp
+		adb/pairing_connection/pairing_connection.cpp
+		${ADB_PAIRING_PROTO_SRCS} ${ADB_PAIRING_PROTO_HDRS})
+
+	if(APPLE)
+		list(APPEND libadb_SOURCES
+			adb/client/usb_osx.cpp)
+	else()
+		list(APPEND libadb_SOURCES
+			adb/client/usb_linux.cpp)
+	endif()
 else()
 	list(APPEND libadb_SOURCES
-		adb/client/usb_linux.cpp)
+		adb/client/adb_wifi_termux_stub.cpp
+		adb/client/usb_termux_stub.cpp
+		adb/client/fastdeploy_termux_stub.cpp)
 endif()
 
 add_library(libadb STATIC ${libadb_SOURCES})
@@ -61,12 +80,41 @@ target_include_directories(libadb PUBLIC
 	core/include
 	core/libcrypto_utils/include
 	core/libcutils/include
+	compat/include
 	libbase/include
-	libziparchive/include)
+	libziparchive/include
+	boringssl/third_party/googletest/googletest/include)
 
 if(ANDROID_TOOLS_USE_BUNDLED_LIBUSB)
 	target_compile_definitions(libadb PUBLIC ANDROID_TOOLS_USE_BUNDLED_LIBUSB)
 	target_include_directories(libadb PUBLIC libusb)
+elseif(NOT ANDROID_TOOLS_TERMUX_ADB)
+	target_include_directories(libadb PUBLIC ${ANDROID_TOOLS_LIBUSB_INCLUDE_DIRS})
+endif()
+
+if(ANDROID_TOOLS_TERMUX_ADB)
+	if(NOT DEFINED TERMUXADB_STATIC_LIB OR TERMUXADB_STATIC_LIB STREQUAL "")
+		message(FATAL_ERROR "ANDROID_TOOLS_TERMUX_ADB requires -DTERMUXADB_STATIC_LIB=/abs/path/to/libtermuxadb.a")
+	endif()
+	target_compile_definitions(libadb PRIVATE ANDROID_TOOLS_TERMUX_ADB=1)
+	target_link_libraries(libadb "${TERMUXADB_STATIC_LIB}")
+endif()
+
+if(ANDROID_TOOLS_TERMUX_ADB AND NOT TARGET libzip)
+	add_library(libzip STATIC
+		libziparchive/zip_archive.cc
+		libziparchive/zip_archive_stream_entry.cc
+		libziparchive/zip_cd_entry_map.cc
+		libziparchive/zip_error.cpp
+		libziparchive/zip_writer.cc)
+	target_compile_definitions(libzip PRIVATE -DZLIB_CONST)
+	target_compile_definitions(libzip PRIVATE -DINCFS_SUPPORT_DISABLED=1)
+	target_include_directories(libzip PUBLIC
+		libziparchive/include
+		libziparchive/incfs_support/include
+		core/base/include
+		boringssl/third_party/googletest/googletest/include)
+	target_link_libraries(libzip PRIVATE libbase z)
 endif()
 
 add_library(libadb_crypto_defaults STATIC
@@ -169,6 +217,7 @@ add_executable(adb
 	  adb/adb.cpp
 	  adb/adb_io.cpp
 	  adb/adb_listeners.cpp
+	  adb/adb_mdns.cpp
 	  adb/adb_trace.cpp
 	  adb/adb_unique_fd.cpp
 	  adb/adb_utils.cpp
@@ -180,11 +229,14 @@ add_executable(adb
 	  adb/socket_spec.cpp
 	  adb/transport.cpp
 	  adb/transport_fd.cpp
-	  adb/client/transport_local.cpp
-	  adb/client/transport_usb.cpp
+	  adb/client/transport_emulator.cpp
 	  adb/types.cpp
 	  ${ADB_KEY_TYPE_PROTO_HDRS})
 
+if(NOT ANDROID_TOOLS_TERMUX_ADB)
+	target_sources(adb PRIVATE adb/client/transport_usb.cpp)
+endif()
+
 target_include_directories(adb PRIVATE
 	core/include libbase/include adb
 	core/libcrypto_utils/include boringssl/include)
@@ -210,7 +262,17 @@ target_link_libraries(adb
 	z
 	PkgConfig::libzstd)
 
-if(ANDROID_TOOLS_USE_BUNDLED_LIBUSB)
+if(ANDROID_TOOLS_TERMUX_ADB AND DEFINED ANDROID_TOOLS_TERMUX_LINK_DIR AND NOT ANDROID_TOOLS_TERMUX_LINK_DIR STREQUAL "")
+	target_link_directories(adb PRIVATE "${ANDROID_TOOLS_TERMUX_LINK_DIR}")
+endif()
+
+if(ANDROID_TOOLS_TERMUX_ADB)
+	if(ANDROID_TOOLS_USE_BUNDLED_LIBUSB)
+		target_link_libraries(adb android_tools_libusb)
+	else()
+		target_link_libraries(adb PkgConfig::libusb-1.0)
+	endif()
+elseif(ANDROID_TOOLS_USE_BUNDLED_LIBUSB)
 	target_link_libraries(adb android_tools_libusb)
 else()
 	target_link_libraries(adb PkgConfig::libusb-1.0)
diff --git a/vendor/CMakeLists.txt b/vendor/CMakeLists.txt
index b92f132..4806eeb 100644
--- a/vendor/CMakeLists.txt
+++ b/vendor/CMakeLists.txt
@@ -88,7 +88,9 @@ pkg_check_modules(libbrotlicommon REQUIRED IMPORTED_TARGET libbrotlicommon)
 pkg_check_modules(libbrotlidec REQUIRED IMPORTED_TARGET libbrotlidec)
 pkg_check_modules(libbrotlienc REQUIRED IMPORTED_TARGET libbrotlienc)
 pkg_check_modules(liblz4 REQUIRED IMPORTED_TARGET liblz4)
-pkg_check_modules(libpcre2-8 REQUIRED IMPORTED_TARGET libpcre2-8)
+if(NOT ANDROID_TOOLS_BUILD_ONLY_ADB)
+	pkg_check_modules(libpcre2-8 REQUIRED IMPORTED_TARGET libpcre2-8)
+endif()
 if(ANDROID_TOOLS_USE_BUNDLED_LIBUSB)
 	include(CMakeLists.libusb.txt)
 	if(ANDROID_TOOLS_LIBUSB_ENABLE_UDEV)
@@ -96,17 +98,33 @@ if(ANDROID_TOOLS_USE_BUNDLED_LIBUSB)
 	endif()
 else()
 	pkg_check_modules(libusb-1.0 REQUIRED IMPORTED_TARGET libusb-1.0)
+	set(ANDROID_TOOLS_LIBUSB_INCLUDE_DIRS ${libusb-1.0_INCLUDE_DIRS})
 endif()
 pkg_check_modules(libzstd REQUIRED IMPORTED_TARGET libzstd)
 set(Protobuf_USE_STATIC_LIBS ON)
-find_package(Protobuf CONFIG)
-find_package(Protobuf REQUIRED)
+set(protobuf_MODULE_COMPATIBLE ON CACHE BOOL "Enable FindProtobuf compatibility helpers" FORCE)
+find_package(Protobuf CONFIG REQUIRED)
 set(THREADS_PREFER_PTHREAD_FLAG ON)
 find_package(Threads REQUIRED)
 
+if(NOT Protobuf_LIBRARIES AND TARGET protobuf::libprotobuf)
+	set(Protobuf_LIBRARIES protobuf::libprotobuf)
+endif()
+
+if(NOT Protobuf_INCLUDE_DIRS AND TARGET protobuf::libprotobuf)
+	get_target_property(Protobuf_INCLUDE_DIRS protobuf::libprotobuf INTERFACE_INCLUDE_DIRECTORIES)
+endif()
+
+set(PROTOBUF_INCLUDE_DIRS ${Protobuf_INCLUDE_DIRS})
+
+if(Protobuf_PROTOC_EXECUTABLE AND NOT TARGET protobuf::protoc)
+	add_executable(protobuf::protoc IMPORTED GLOBAL)
+endif()
+
 if(Protobuf_PROTOC_EXECUTABLE)
   set_target_properties(protobuf::protoc PROPERTIES
     IMPORTED_LOCATION_RELEASE "${Protobuf_PROTOC_EXECUTABLE}"
+    IMPORTED_LOCATION "${Protobuf_PROTOC_EXECUTABLE}"
   )
 endif()
 
@@ -118,42 +136,50 @@ include_directories(${CMAKE_CURRENT_BINARY_DIR})
 
 include(CMakeLists.libbase.txt)
 include(CMakeLists.adb.txt)
-include(CMakeLists.sparse.txt)
-include(CMakeLists.fastboot.txt)
-include(CMakeLists.f2fstools.txt)
-include(CMakeLists.mke2fs.txt)
-include(CMakeLists.partition.txt)
-include(CMakeLists.mkbootimg.txt)
-include(CMakeLists.libufdt.txt)
-include(CMakeLists.avb.txt)
+if(NOT ANDROID_TOOLS_BUILD_ONLY_ADB)
+	include(CMakeLists.sparse.txt)
+	include(CMakeLists.fastboot.txt)
+	include(CMakeLists.f2fstools.txt)
+	include(CMakeLists.mke2fs.txt)
+	include(CMakeLists.partition.txt)
+	include(CMakeLists.mkbootimg.txt)
+	include(CMakeLists.libufdt.txt)
+	include(CMakeLists.avb.txt)
+endif()
 
 # Targets which should be installed by `make install`.
-install(TARGETS
-	"${ANDROID_MKE2FS_NAME}"
-	adb
-	append2simg
-	fastboot
-	img2simg
-	lpadd
-	lpdump
-	lpflash
-	lpmake
-	lpunpack
-	make_f2fs
-	sload_f2fs
-	simg2img
-	ext2simg
-	DESTINATION bin)
-
-if(NOT APPLE)
+if(ANDROID_TOOLS_BUILD_ONLY_ADB)
+	install(TARGETS adb DESTINATION bin)
+else()
 	install(TARGETS
-		e2fsdroid
+		"${ANDROID_MKE2FS_NAME}"
+		adb
+		append2simg
+		fastboot
+		img2simg
+		lpadd
+		lpdump
+		lpflash
+		lpmake
+		lpunpack
+		make_f2fs
+		sload_f2fs
+		simg2img
+		ext2simg
 		DESTINATION bin)
+
+	if(NOT APPLE)
+		install(TARGETS
+			e2fsdroid
+			DESTINATION bin)
+	endif()
 endif()
 
 # Install common completion files.
 install(FILES adb/adb.bash RENAME adb DESTINATION "${COMPLETION_COMMON_DIR}")
-install(FILES core/fastboot/fastboot.bash RENAME fastboot DESTINATION "${COMPLETION_COMMON_DIR}")
+if(NOT ANDROID_TOOLS_BUILD_ONLY_ADB)
+	install(FILES core/fastboot/fastboot.bash RENAME fastboot DESTINATION "${COMPLETION_COMMON_DIR}")
+endif()
 
 # Install license files.
 # Disabled for now, see https://github.com/nmeum/android-tools/issues/30#issuecomment-855365636
